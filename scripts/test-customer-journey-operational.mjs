import assert from 'node:assert/strict'
import {JOURNEY_EVENTS,journeyMessage,shouldSendJourneyWhatsApp,eventForFinalAction,trackingUrl} from '../src/lib/customerJourney.js'
import {advanceOperationalJourney,effectiveJourneyEvent,markJourneyFinal,finalActionLabel,journeyEligible} from '../src/lib/customerJourneyOperational.js'
const enabled=(order,at='2026-09-02T10:00:00.000Z')=>({...order,journey:{enabled:true,stage:JOURNEY_EVENTS.CONFIRMED,confirmedAt:at,whatsappConfirmedStatus:'simulated-private'}})
const makeOrder=(id,number,items,extra={})=>enabled({id,number,client:`Cliente ${number}`,delivery:'2026-09-02',deliveryType:'Vía Cargo',status:'Ingresado',createdAt:'2026-09-02T10:00:00.000Z',items,...extra})
const rows=(...items)=>()=>items
let db={orders:[makeOrder('a',1,[{figure:'A',qty:2,inventoryTracked:true}]),makeOrder('b',2,[{figure:'B',qty:2,inventoryTracked:true}])],cutBatches:[]}
let result=advanceOperationalJourney(db,'2026-09-02T12:00:00.000Z',{stockRowsFn:rows({figure:'A',cut:0,inCut:2},{figure:'B',cut:0,inCut:0})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.CONFIRMED)
db.cutBatches=[{id:'p1',journeyManaged:true,date:'2026-09-02',deliveryDates:['2026-09-02'],status:'En corte',sentToCutAt:'2026-09-02T12:00:00.000Z',items:[{figure:'A',qty:2},{figure:'B',qty:1}]}]
result=advanceOperationalJourney(db,'2026-09-02T12:01:00.000Z',{stockRowsFn:rows({figure:'A',cut:0,inCut:2},{figure:'B',cut:0,inCut:1})});db={...db,orders:result.orders};assert.equal(db.orders[0].journey.stage,JOURNEY_EVENTS.PRODUCTION_CUT);assert.equal(db.orders[1].journey.stage,JOURNEY_EVENTS.CONFIRMED)
db.cutBatches.push({id:'p2',journeyManaged:true,date:'2026-09-02',deliveryDates:['2026-09-02'],status:'En corte',sentToCutAt:'2026-09-02T12:10:00.000Z',items:[{figure:'B',qty:1}]});result=advanceOperationalJourney(db,'2026-09-02T12:11:00.000Z',{stockRowsFn:rows({figure:'A',cut:0,inCut:2},{figure:'B',cut:0,inCut:2})});db={...db,orders:result.orders};assert.equal(db.orders[1].journey.stage,JOURNEY_EVENTS.PRODUCTION_CUT)
db.cutBatches[0]={...db.cutBatches[0],status:'Terminada',finishedAt:'2026-09-02T13:00:00.000Z'};result=advanceOperationalJourney(db,'2026-09-02T13:00:00.000Z',{stockRowsFn:rows({figure:'A',cut:2,inCut:0},{figure:'B',cut:1,inCut:1})});db={...db,orders:result.orders};assert.equal(db.orders[0].journey.cutCompletedAt,'2026-09-02T13:00:00.000Z');assert.equal(db.orders[1].journey.cutCompletedAt,undefined)
db.cutBatches[1]={...db.cutBatches[1],status:'Terminada',finishedAt:'2026-09-02T13:30:00.000Z'};result=advanceOperationalJourney(db,'2026-09-02T13:30:00.000Z',{stockRowsFn:rows({figure:'A',cut:2,inCut:0},{figure:'B',cut:2,inCut:0})});db={...db,orders:result.orders};assert.equal(db.orders[1].journey.cutCompletedAt,'2026-09-02T13:30:00.000Z')
assert.equal(effectiveJourneyEvent(db.orders[1],'2026-09-02T16:29:59.000Z'),JOURNEY_EVENTS.PRODUCTION_CUT);assert.equal(effectiveJourneyEvent(db.orders[1],'2026-09-02T16:30:00.000Z'),JOURNEY_EVENTS.PACKING)
const reloadedOrder=JSON.parse(JSON.stringify(db.orders[1]));result=advanceOperationalJourney({...db,orders:[reloadedOrder]},'2026-09-02T16:31:00.000Z',{stockRowsFn:rows({figure:'B',cut:2,inCut:0})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.PACKING)
const tooEarly=markJourneyFinal({...db.orders[0],journey:{...db.orders[0].journey,cutCompletedAt:'2026-09-02T15:00:00.000Z'}},'2026-09-02T16:00:00.000Z');assert.notEqual(tooEarly.journey.stage,JOURNEY_EVENTS.DISPATCHED)
let final=markJourneyFinal(reloadedOrder,'2026-09-02T17:00:00.000Z');assert.equal(final.journey.stage,JOURNEY_EVENTS.DISPATCHED);assert.equal(finalActionLabel(final),'DESPACHADO');assert.deepEqual(markJourneyFinal(final,'2026-09-02T17:01:00.000Z'),final);assert.equal(eventForFinalAction({...reloadedOrder,deliveryType:'Retiro en el local'}),JOURNEY_EVENTS.READY_PICKUP)
const legacyOrder={id:'legacy',number:99,delivery:'2026-09-02',status:'Ingresado',items:[{figure:'A',qty:1,inventoryTracked:true}]};result=advanceOperationalJourney({orders:[legacyOrder],cutBatches:[]},'2026-09-02T20:00:00.000Z',{stockRowsFn:rows({figure:'A',cut:9,inCut:0})});assert.equal(result.orders[0].journey.enabled,true);assert.equal(journeyEligible({...legacyOrder,delivery:'2026-09-01'},'2026-09-02T20:00:00.000Z'),false)
assert.equal(shouldSendJourneyWhatsApp(JOURNEY_EVENTS.CONFIRMED),true);assert.equal(shouldSendJourneyWhatsApp(JOURNEY_EVENTS.PRODUCTION_CUT),false);assert.equal(shouldSendJourneyWhatsApp(JOURNEY_EVENTS.PACKING),false);assert.equal(shouldSendJourneyWhatsApp(JOURNEY_EVENTS.DISPATCHED),true);assert.equal(journeyMessage(final,JOURNEY_EVENTS.PACKING),'');assert.match(journeyMessage(final,JOURNEY_EVENTS.DISPATCHED,{reviewUrl:'https://catalogo.example/opiniones'}),/Esperamos que disfrutes/);assert.equal(trackingUrl({trackingToken:'token-prueba'}),'https://tu-vida-en-tinta-catalogo-v2.vercel.app/p/token-prueba')
let stockDb={orders:[makeOrder('stock',10,[{figure:'Stock',qty:2,inventoryTracked:true}])],cutBatches:[]};result=advanceOperationalJourney(stockDb,'2026-09-02T12:00:00.000Z',{stockRowsFn:rows({figure:'Stock',cut:2,inCut:0})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.PACKING)
let deficitDb={orders:[makeOrder('deficit',11,[{figure:'Falta',qty:2,inventoryTracked:true}])],cutBatches:[]};result=advanceOperationalJourney(deficitDb,'2026-09-02T12:00:00.000Z',{stockRowsFn:rows({figure:'Falta',cut:1,inCut:0})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.CONFIRMED)
let chronoDb={orders:[makeOrder('first',12,[{figure:'Uno',qty:1,inventoryTracked:true}],{delivery:'2026-09-02'}),makeOrder('second',13,[{figure:'Uno',qty:1,inventoryTracked:true}],{delivery:'2026-09-03'})],cutBatches:[]};result=advanceOperationalJourney(chronoDb,'2026-09-02T12:00:00.000Z',{stockRowsFn:rows({figure:'Uno',cut:1,inCut:0})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.PACKING);assert.equal(result.orders[1].journey.stage,JOURNEY_EVENTS.CONFIRMED)
// Componentes: stockRows informa projected neto 0 con ordered 1 => hay 1 unidad futura para cubrir ese pedido.
let componentDb={orders:[makeOrder('parts',14,[{figure:'Corazon',qty:1,inventoryTracked:true}])],cutBatches:[{id:'parts-batch',journeyManaged:true,date:'2026-09-02',deliveryDates:['2026-09-02'],status:'En corte',sentToCutAt:'2026-09-02T12:00:00.000Z',items:[{figure:'Corazon',component:'tapa',qty:1}]}]};result=advanceOperationalJourney(componentDb,'2026-09-02T12:01:00.000Z',{stockRowsFn:rows({figure:'Corazon',cut:0,ordered:1,inCut:0,projected:0})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.PRODUCTION_CUT)
let serviceDb={orders:[makeOrder('service',15,[{figure:'Servicio',qty:1,inventoryTracked:false}])],cutBatches:[]};result=advanceOperationalJourney(serviceDb,'2026-09-02T12:00:00.000Z',{stockRowsFn:rows()});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.CONFIRMED)

// Guardas nuevas: una placa de otra fecha o una cobertura parcial nunca puede adelantar un pedido.
let unrelatedDb={orders:[makeOrder('unrelated',16,[{figure:'Misma',qty:2,inventoryTracked:true}],{delivery:'2026-09-02'})],cutBatches:[{id:'other-date',journeyManaged:true,date:'2026-09-03',deliveryDates:['2026-09-03'],status:'En corte',sentToCutAt:'2026-09-02T12:00:00.000Z',items:[{figure:'Misma',qty:2}]}]};result=advanceOperationalJourney(unrelatedDb,'2026-09-02T12:01:00.000Z',{stockRowsFn:rows({figure:'Misma',cut:0,inCut:2})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.CONFIRMED)
let partialDb={orders:[makeOrder('partial',17,[{figure:'X',qty:2,inventoryTracked:true},{figure:'Y',qty:2,inventoryTracked:true}])],cutBatches:[{id:'partial-batch',journeyManaged:true,date:'2026-09-02',deliveryDates:['2026-09-02'],status:'En corte',sentToCutAt:'2026-09-02T12:00:00.000Z',items:[{figure:'X',qty:2},{figure:'Y',qty:1}]}]};result=advanceOperationalJourney(partialDb,'2026-09-02T12:01:00.000Z',{stockRowsFn:rows({figure:'X',cut:0,inCut:2},{figure:'Y',cut:0,inCut:1})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.CONFIRMED)
partialDb.cutBatches.push({id:'partial-complete',journeyManaged:true,date:'2026-09-02',deliveryDates:['2026-09-02'],status:'En corte',sentToCutAt:'2026-09-02T12:10:00.000Z',items:[{figure:'Y',qty:1}]});result=advanceOperationalJourney(partialDb,'2026-09-02T12:11:00.000Z',{stockRowsFn:rows({figure:'X',cut:0,inCut:2},{figure:'Y',cut:0,inCut:2})});assert.equal(result.orders[0].journey.stage,JOURNEY_EVENTS.PRODUCTION_CUT)
console.log('customer journey predeploy: 15/15 OK')
