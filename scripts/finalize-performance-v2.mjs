import fs from 'node:fs'
function replaceOnce(source,needle,replacement,label){if(source.includes(replacement))return source;const first=source.indexOf(needle);if(first<0){console.log(`performance-v2: ${label} ya evolucionó; se conserva`);return source}if(source.indexOf(needle,first+needle.length)>=0)throw new Error(`performance-v2: ${label} aparece más de una vez`);return source.replace(needle,replacement)}
const stockPath='src/pages/StockBase.jsx';let stock=fs.readFileSync(stockPath,'utf8');stock=replaceOnce(stock,"  const rows=stockRows(db).filter(r=>r.figure.toLowerCase().includes(search.toLowerCase()))","  const allStockRows=useMemo(()=>stockRows(db),[db])\n  const rows=useMemo(()=>allStockRows.filter(r=>r.figure.toLowerCase().includes(search.toLowerCase())),[allStockRows,search])",'cálculo StockBase');stock=replaceOnce(stock,"  const rowByFigure=useMemo(()=>Object.fromEntries(stockRows(db).map(r=>[r.figure,r])),[db])","  const rowByFigure=useMemo(()=>Object.fromEntries(allStockRows.map(r=>[r.figure,r])),[allStockRows])",'rowByFigure');fs.writeFileSync(stockPath,stock)
const invPath='src/lib/inventory.js';let inv=fs.readFileSync(invPath,'utf8');inv=replaceOnce(inv,"const CUT_REPAIR_CUTOFF='2026-08-14T23:59:59'","const CUT_REPAIR_CUTOFF='2026-08-14T23:59:59'\nconst finishedProductionCache=new WeakMap()",'cache declaración');inv=replaceOnce(inv,"function missingFinishedBatchProduction(db){\n  const complete={},components={}","function missingFinishedBatchProduction(db){\n  const cached=finishedProductionCache.get(db)\n  if(cached)return cached\n  const complete={},components={}",'cache entrada');inv=replaceOnce(inv,"  return {complete,components}\n}\n\nexport function manualBalance(db){","  const result={complete,components}\n  finishedProductionCache.set(db,result)\n  return result\n}\n\nexport function manualBalance(db){",'cache salida');fs.writeFileSync(invPath,inv);console.log('PERFORMANCE V2 OK · idempotente')
