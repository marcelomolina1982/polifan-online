services:
  - type: web
    name: polifan-cnc-solver-lab
    runtime: docker
    plan: free
    dockerfilePath: ./solver-service/Dockerfile
    dockerContext: ./solver-service
    healthCheckPath: /health
    autoDeployTrigger: commit
