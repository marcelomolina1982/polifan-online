name: Fix Sparrow component planning
on:
  push:
    branches: [app-v2-parallel]
    paths: ['.github/workflows/fix-sparrow-components.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: app-v2-parallel
      - name: Apply Sparrow component fix
        run: python .github/scripts/fix-sparrow-components.py
      - name: Validate build
        run: npm ci && npm run build
      - name: Commit validated fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add scripts/prepare-v25.0.25.mjs
          git diff --cached --quiet || git commit -m "Mostrar y conservar componentes reales en Sparrow V1.13"
          git push origin HEAD:app-v2-parallel
# activated component-aware V1.13
